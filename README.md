# Gtk.Popover arrow border transparency issue demo

You'll need the following dependencies:

* meson >= 0.43.0
* valac

Run:
```
    meson build --prefix=/usr
    cd build
    ninja
    ./popover-arrow-border-transparency
```
